import { test, expect } from '@playwright/test';

test('epsilon ui test 01', async ({ page }) => {
  await console.log('epsilon test module 01');
});

test('epsilon ui test 02', async ({ page }) => {
  await console.log('epsilon test module 02');
});

test('epsilon ui test 03', async ({ page }) => {
  await console.log('epsilon test module 03');
});

test('epsilon ui test 04', async ({ page }) => {
  await console.log('epsilon test module 04');
});


